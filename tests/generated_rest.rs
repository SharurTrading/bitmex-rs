//! Generated, deterministic REST loopback fixtures.
// @generated by tools/generate.py. Do not edit.
#![allow(clippy::expect_used)]
mod support;
use bitmex_client::generated::models::*;
use bitmex_client::{ApiCredentials, Client, Environment, PathId};

#[tokio::test]
async fn address_get_success_and_rejection() {
    let (url, received) = support::serve(200, r#"[]"#).await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.address_get().await;
    assert!(
        result.is_ok(),
        "success fixture for GET /api/v1/address: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(request.starts_with("GET /api/v1/address"), "{request}");
    assert!(request.contains("api-signature:"), "{request}");

    let (url, received) = support::serve(
        400,
        r#"{"error":{"name":"ValidationError","message":"fixture"}}"#,
    )
    .await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.address_get().await;
    assert!(
        matches!(
            result,
            Err(bitmex_client::OperationError::Rejected { status: 400, .. })
        ),
        "rejection fixture: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(request.starts_with("GET /api/v1/address"), "{request}");
}

#[tokio::test]
async fn address_new_success_and_rejection() {
    let (url, received) = support::serve(
        200,
        r#"{"address":"fixture","name":"fixture","network":"fixture"}"#,
    )
    .await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.address_new().await;
    assert!(
        result.is_ok(),
        "success fixture for POST /api/v1/address: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(request.starts_with("POST /api/v1/address"), "{request}");
    assert!(request.contains("api-signature:"), "{request}");

    let (url, received) = support::serve(
        400,
        r#"{"error":{"name":"ValidationError","message":"fixture"}}"#,
    )
    .await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.address_new().await;
    assert!(
        matches!(
            result,
            Err(bitmex_client::OperationError::Rejected { status: 400, .. })
        ),
        "rejection fixture: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(request.starts_with("POST /api/v1/address"), "{request}");
}

#[tokio::test]
async fn address_update_success_and_rejection() {
    let (url, received) = support::serve(200, r#"false"#).await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.address_update().await;
    assert!(
        result.is_ok(),
        "success fixture for PUT /api/v1/address: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(request.starts_with("PUT /api/v1/address"), "{request}");
    assert!(request.contains("api-signature:"), "{request}");

    let (url, received) = support::serve(
        400,
        r#"{"error":{"name":"ValidationError","message":"fixture"}}"#,
    )
    .await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.address_update().await;
    assert!(
        matches!(
            result,
            Err(bitmex_client::OperationError::Rejected { status: 400, .. })
        ),
        "rejection fixture: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(request.starts_with("PUT /api/v1/address"), "{request}");
}

#[tokio::test]
async fn address_config_get_success_and_rejection() {
    let (url, received) = support::serve(200, r#"{"userId":1}"#).await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.address_config_get().await;
    assert!(
        result.is_ok(),
        "success fixture for GET /api/v1/addressConfig: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("GET /api/v1/addressConfig"),
        "{request}"
    );
    assert!(request.contains("api-signature:"), "{request}");

    let (url, received) = support::serve(
        400,
        r#"{"error":{"name":"ValidationError","message":"fixture"}}"#,
    )
    .await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.address_config_get().await;
    assert!(
        matches!(
            result,
            Err(bitmex_client::OperationError::Rejected { status: 400, .. })
        ),
        "rejection fixture: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("GET /api/v1/addressConfig"),
        "{request}"
    );
}

#[tokio::test]
async fn announcement_get_success_and_rejection() {
    let query = AnnouncementGetQuery::default();
    let (url, received) = support::serve(200, r#"[]"#).await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.announcement_get(&query).await;
    assert!(
        result.is_ok(),
        "success fixture for GET /api/v1/announcement: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(request.starts_with("GET /api/v1/announcement"), "{request}");
    assert!(request.contains("api-signature:"), "{request}");

    let (url, received) = support::serve(
        400,
        r#"{"error":{"name":"ValidationError","message":"fixture"}}"#,
    )
    .await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.announcement_get(&query).await;
    assert!(
        matches!(
            result,
            Err(bitmex_client::OperationError::Rejected { status: 400, .. })
        ),
        "rejection fixture: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(request.starts_with("GET /api/v1/announcement"), "{request}");
}

#[tokio::test]
async fn announcement_get_urgent_success_and_rejection() {
    let (url, received) = support::serve(200, r#"[]"#).await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.announcement_get_urgent().await;
    assert!(
        result.is_ok(),
        "success fixture for GET /api/v1/announcement/urgent: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("GET /api/v1/announcement/urgent"),
        "{request}"
    );
    assert!(request.contains("api-signature:"), "{request}");

    let (url, received) = support::serve(
        400,
        r#"{"error":{"name":"ValidationError","message":"fixture"}}"#,
    )
    .await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.announcement_get_urgent().await;
    assert!(
        matches!(
            result,
            Err(bitmex_client::OperationError::Rejected { status: 400, .. })
        ),
        "rejection fixture: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("GET /api/v1/announcement/urgent"),
        "{request}"
    );
}

#[tokio::test]
async fn api_key_get_success_and_rejection() {
    let query = ApiKeyGetQuery::default();
    let (url, received) = support::serve(200, r#"[]"#).await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.api_key_get(&query).await;
    assert!(
        result.is_ok(),
        "success fixture for GET /api/v1/apiKey: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(request.starts_with("GET /api/v1/apiKey"), "{request}");
    assert!(request.contains("api-signature:"), "{request}");

    let (url, received) = support::serve(
        400,
        r#"{"error":{"name":"ValidationError","message":"fixture"}}"#,
    )
    .await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.api_key_get(&query).await;
    assert!(
        matches!(
            result,
            Err(bitmex_client::OperationError::Rejected { status: 400, .. })
        ),
        "rejection fixture: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(request.starts_with("GET /api/v1/apiKey"), "{request}");
}

#[tokio::test]
async fn api_key_self_success_and_rejection() {
    let (url, received) = support::serve(
        200,
        r#"{"id":"fixture","secret":"fixture","name":"fixture","nonce":1,"userId":1}"#,
    )
    .await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.api_key_self().await;
    assert!(
        result.is_ok(),
        "success fixture for GET /api/v1/apiKey/self: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(request.starts_with("GET /api/v1/apiKey/self"), "{request}");
    assert!(request.contains("api-signature:"), "{request}");

    let (url, received) = support::serve(
        400,
        r#"{"error":{"name":"ValidationError","message":"fixture"}}"#,
    )
    .await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.api_key_self().await;
    assert!(
        matches!(
            result,
            Err(bitmex_client::OperationError::Rejected { status: 400, .. })
        ),
        "rejection fixture: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(request.starts_with("GET /api/v1/apiKey/self"), "{request}");
}

#[tokio::test]
async fn account_range_success_and_rejection() {
    let (url, received) = support::serve(
        200,
        r#"{"apiBrokerId":1,"currentAccountId":1,"mainAccountId":1,"maxAccountId":1}"#,
    )
    .await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.account_range().await;
    assert!(
        result.is_ok(),
        "success fixture for GET /api/v1/broker/accountRange: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("GET /api/v1/broker/accountRange"),
        "{request}"
    );
    assert!(request.contains("api-signature:"), "{request}");

    let (url, received) = support::serve(
        400,
        r#"{"error":{"name":"ValidationError","message":"fixture"}}"#,
    )
    .await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.account_range().await;
    assert!(
        matches!(
            result,
            Err(bitmex_client::OperationError::Rejected { status: 400, .. })
        ),
        "rejection fixture: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("GET /api/v1/broker/accountRange"),
        "{request}"
    );
}

#[tokio::test]
async fn create_success_and_rejection() {
    let body: CreateBody =
        serde_json::from_str(r#"{"account":1,"logged":"fixture","typ":"Exchange"}"#)
            .expect("body fixture");
    let (url, received) = support::serve(200, r#"{"account":1}"#).await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.create(&body).await;
    assert!(
        result.is_ok(),
        "success fixture for POST /api/v1/broker/accounts/create: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("POST /api/v1/broker/accounts/create"),
        "{request}"
    );
    assert!(request.contains("api-signature:"), "{request}");

    let (url, received) = support::serve(
        400,
        r#"{"error":{"name":"ValidationError","message":"fixture"}}"#,
    )
    .await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.create(&body).await;
    assert!(
        matches!(
            result,
            Err(bitmex_client::OperationError::Rejected { status: 400, .. })
        ),
        "rejection fixture: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("POST /api/v1/broker/accounts/create"),
        "{request}"
    );
}

#[tokio::test]
async fn update_success_and_rejection() {
    let body: UpdateBody = serde_json::from_str(r#"{"account":1}"#).expect("body fixture");
    let (url, received) = support::serve(200, r#"{"account":1}"#).await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.update(&body).await;
    assert!(
        result.is_ok(),
        "success fixture for POST /api/v1/broker/accounts/update: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("POST /api/v1/broker/accounts/update"),
        "{request}"
    );
    assert!(request.contains("api-signature:"), "{request}");

    let (url, received) = support::serve(
        400,
        r#"{"error":{"name":"ValidationError","message":"fixture"}}"#,
    )
    .await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.update(&body).await;
    assert!(
        matches!(
            result,
            Err(bitmex_client::OperationError::Rejected { status: 400, .. })
        ),
        "rejection fixture: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("POST /api/v1/broker/accounts/update"),
        "{request}"
    );
}

#[tokio::test]
async fn add_new_account_success_and_rejection() {
    let body: AddNewAccountBody =
        serde_json::from_str(r#"{"timestamp":"fixture","type":"Exchange"}"#).expect("body fixture");
    let (url, received) = support::serve(200, r#"{"createdAccountId":1,"type":"Exchange"}"#).await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.add_new_account(&body).await;
    assert!(
        result.is_ok(),
        "success fixture for POST /api/v1/broker/addNewAccount: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("POST /api/v1/broker/addNewAccount"),
        "{request}"
    );
    assert!(request.contains("api-signature:"), "{request}");

    let (url, received) = support::serve(
        400,
        r#"{"error":{"name":"ValidationError","message":"fixture"}}"#,
    )
    .await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.add_new_account(&body).await;
    assert!(
        matches!(
            result,
            Err(bitmex_client::OperationError::Rejected { status: 400, .. })
        ),
        "rejection fixture: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("POST /api/v1/broker/addNewAccount"),
        "{request}"
    );
}

#[tokio::test]
async fn change_account_restriction_success_and_rejection() {
    let body: ChangeAccountRestrictionBody =
        serde_json::from_str(r#"{"accountId":1,"state":"Restricted"}"#).expect("body fixture");
    let (url, received) = support::serve(
        200,
        r#"{"accountId":1,"state":"Restricted","timestamp":"fixture"}"#,
    )
    .await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.change_account_restriction(&body).await;
    assert!(
        result.is_ok(),
        "success fixture for POST /api/v1/broker/changeAccountRestriction: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("POST /api/v1/broker/changeAccountRestriction"),
        "{request}"
    );
    assert!(request.contains("api-signature:"), "{request}");

    let (url, received) = support::serve(
        400,
        r#"{"error":{"name":"ValidationError","message":"fixture"}}"#,
    )
    .await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.change_account_restriction(&body).await;
    assert!(
        matches!(
            result,
            Err(bitmex_client::OperationError::Rejected { status: 400, .. })
        ),
        "rejection fixture: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("POST /api/v1/broker/changeAccountRestriction"),
        "{request}"
    );
}

#[tokio::test]
async fn create_brokerage_fee_tier_success_and_rejection() {
    let body: CreateBrokerageFeeTierBody =
        serde_json::from_str(r#"{"account":1,"brokerMakerFee":1,"brokerTakerFee":1}"#)
            .expect("body fixture");
    let (url, received) = support::serve(
        200,
        r#"{"account":1,"brokerMakerFee":1,"brokerTakerFee":1,"feeGroupID":1}"#,
    )
    .await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.create_brokerage_fee_tier(&body).await;
    assert!(
        result.is_ok(),
        "success fixture for POST /api/v1/broker/discounts/createBrokerageFeeTier: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("POST /api/v1/broker/discounts/createBrokerageFeeTier"),
        "{request}"
    );
    assert!(request.contains("api-signature:"), "{request}");

    let (url, received) = support::serve(
        400,
        r#"{"error":{"name":"ValidationError","message":"fixture"}}"#,
    )
    .await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.create_brokerage_fee_tier(&body).await;
    assert!(
        matches!(
            result,
            Err(bitmex_client::OperationError::Rejected { status: 400, .. })
        ),
        "rejection fixture: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("POST /api/v1/broker/discounts/createBrokerageFeeTier"),
        "{request}"
    );
}

#[tokio::test]
async fn update_broker_fee_account_map_success_and_rejection() {
    let body: UpdateBrokerFeeAccountMapBody =
        serde_json::from_str(r#"{"account":1,"feeAccount":1}"#).expect("body fixture");
    let (url, received) = support::serve(200, r#"{"account":1,"feeAccount":1}"#).await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.update_broker_fee_account_map(&body).await;
    assert!(
        result.is_ok(),
        "success fixture for POST /api/v1/broker/discounts/updateBrokerFeeAccountMap: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("POST /api/v1/broker/discounts/updateBrokerFeeAccountMap"),
        "{request}"
    );
    assert!(request.contains("api-signature:"), "{request}");

    let (url, received) = support::serve(
        400,
        r#"{"error":{"name":"ValidationError","message":"fixture"}}"#,
    )
    .await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.update_broker_fee_account_map(&body).await;
    assert!(
        matches!(
            result,
            Err(bitmex_client::OperationError::Rejected { status: 400, .. })
        ),
        "rejection fixture: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("POST /api/v1/broker/discounts/updateBrokerFeeAccountMap"),
        "{request}"
    );
}

#[tokio::test]
async fn update_brokerage_fee_tier_success_and_rejection() {
    let body: UpdateBrokerageFeeTierBody = serde_json::from_str(
        r#"{"account":1,"brokerMakerFee":1,"brokerTakerFee":1,"feeGroupID":1}"#,
    )
    .expect("body fixture");
    let (url, received) = support::serve(
        200,
        r#"{"account":1,"brokerMakerFee":1,"brokerTakerFee":1,"feeGroupID":1}"#,
    )
    .await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.update_brokerage_fee_tier(&body).await;
    assert!(
        result.is_ok(),
        "success fixture for POST /api/v1/broker/discounts/updateBrokerageFeeTier: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("POST /api/v1/broker/discounts/updateBrokerageFeeTier"),
        "{request}"
    );
    assert!(request.contains("api-signature:"), "{request}");

    let (url, received) = support::serve(
        400,
        r#"{"error":{"name":"ValidationError","message":"fixture"}}"#,
    )
    .await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.update_brokerage_fee_tier(&body).await;
    assert!(
        matches!(
            result,
            Err(bitmex_client::OperationError::Rejected { status: 400, .. })
        ),
        "rejection fixture: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("POST /api/v1/broker/discounts/updateBrokerageFeeTier"),
        "{request}"
    );
}

#[tokio::test]
async fn update_derivatives_fee_discount_success_and_rejection() {
    let body: UpdateDerivativesFeeDiscountBody =
        serde_json::from_str(r#"{"accounts":1,"currency":[]}"#).expect("body fixture");
    let (url, received) = support::serve(200, r#"{"account":1,"currency":"fixture"}"#).await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.update_derivatives_fee_discount(&body).await;
    assert!(
        result.is_ok(),
        "success fixture for POST /api/v1/broker/discounts/updateDerivativesFeeDiscount: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("POST /api/v1/broker/discounts/updateDerivativesFeeDiscount"),
        "{request}"
    );
    assert!(request.contains("api-signature:"), "{request}");

    let (url, received) = support::serve(
        400,
        r#"{"error":{"name":"ValidationError","message":"fixture"}}"#,
    )
    .await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.update_derivatives_fee_discount(&body).await;
    assert!(
        matches!(
            result,
            Err(bitmex_client::OperationError::Rejected { status: 400, .. })
        ),
        "rejection fixture: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("POST /api/v1/broker/discounts/updateDerivativesFeeDiscount"),
        "{request}"
    );
}

#[tokio::test]
async fn update_spot_fee_discount_success_and_rejection() {
    let body: UpdateSpotFeeDiscountBody =
        serde_json::from_str(r#"{"accounts":1,"currency":[]}"#).expect("body fixture");
    let (url, received) = support::serve(200, r#"[]"#).await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.update_spot_fee_discount(&body).await;
    assert!(
        result.is_ok(),
        "success fixture for POST /api/v1/broker/discounts/updateSpotFeeDiscount: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("POST /api/v1/broker/discounts/updateSpotFeeDiscount"),
        "{request}"
    );
    assert!(request.contains("api-signature:"), "{request}");

    let (url, received) = support::serve(
        400,
        r#"{"error":{"name":"ValidationError","message":"fixture"}}"#,
    )
    .await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.update_spot_fee_discount(&body).await;
    assert!(
        matches!(
            result,
            Err(bitmex_client::OperationError::Rejected { status: 400, .. })
        ),
        "rejection fixture: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("POST /api/v1/broker/discounts/updateSpotFeeDiscount"),
        "{request}"
    );
}

#[tokio::test]
async fn update_user_fee_tier_map_success_and_rejection() {
    let body: UpdateUserFeeTierMapBody =
        serde_json::from_str(r#"{"account":1,"feeGroupID":1}"#).expect("body fixture");
    let (url, received) = support::serve(200, r#"{"account":1,"feeGroupID":1}"#).await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.update_user_fee_tier_map(&body).await;
    assert!(
        result.is_ok(),
        "success fixture for POST /api/v1/broker/discounts/updateUserFeeTierMap: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("POST /api/v1/broker/discounts/updateUserFeeTierMap"),
        "{request}"
    );
    assert!(request.contains("api-signature:"), "{request}");

    let (url, received) = support::serve(
        400,
        r#"{"error":{"name":"ValidationError","message":"fixture"}}"#,
    )
    .await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.update_user_fee_tier_map(&body).await;
    assert!(
        matches!(
            result,
            Err(bitmex_client::OperationError::Rejected { status: 400, .. })
        ),
        "rejection fixture: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("POST /api/v1/broker/discounts/updateUserFeeTierMap"),
        "{request}"
    );
}

#[tokio::test]
async fn chat_get_success_and_rejection() {
    let query = ChatGetQuery::default();
    let (url, received) = support::serve(200, r#"[]"#).await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.chat_get(&query).await;
    assert!(
        result.is_ok(),
        "success fixture for GET /api/v1/chat: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(request.starts_with("GET /api/v1/chat"), "{request}");
    assert!(request.contains("api-signature:"), "{request}");

    let (url, received) = support::serve(
        400,
        r#"{"error":{"name":"ValidationError","message":"fixture"}}"#,
    )
    .await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.chat_get(&query).await;
    assert!(
        matches!(
            result,
            Err(bitmex_client::OperationError::Rejected { status: 400, .. })
        ),
        "rejection fixture: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(request.starts_with("GET /api/v1/chat"), "{request}");
}

#[tokio::test]
async fn chat_new_success_and_rejection() {
    let (url, received) = support::serve(
        200,
        r#"{"date":"fixture","user":"fixture","message":"fixture","html":"fixture"}"#,
    )
    .await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.chat_new().await;
    assert!(
        result.is_ok(),
        "success fixture for POST /api/v1/chat: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(request.starts_with("POST /api/v1/chat"), "{request}");
    assert!(request.contains("api-signature:"), "{request}");

    let (url, received) = support::serve(
        400,
        r#"{"error":{"name":"ValidationError","message":"fixture"}}"#,
    )
    .await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.chat_new().await;
    assert!(
        matches!(
            result,
            Err(bitmex_client::OperationError::Rejected { status: 400, .. })
        ),
        "rejection fixture: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(request.starts_with("POST /api/v1/chat"), "{request}");
}

#[tokio::test]
async fn chat_get_channels_success_and_rejection() {
    let (url, received) = support::serve(200, r#"[]"#).await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.chat_get_channels().await;
    assert!(
        result.is_ok(),
        "success fixture for GET /api/v1/chat/channels: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("GET /api/v1/chat/channels"),
        "{request}"
    );
    assert!(request.contains("api-signature:"), "{request}");

    let (url, received) = support::serve(
        400,
        r#"{"error":{"name":"ValidationError","message":"fixture"}}"#,
    )
    .await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.chat_get_channels().await;
    assert!(
        matches!(
            result,
            Err(bitmex_client::OperationError::Rejected { status: 400, .. })
        ),
        "rejection fixture: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("GET /api/v1/chat/channels"),
        "{request}"
    );
}

#[tokio::test]
async fn chat_get_connected_success_and_rejection() {
    let (url, received) = support::serve(200, r#"{}"#).await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.chat_get_connected().await;
    assert!(
        result.is_ok(),
        "success fixture for GET /api/v1/chat/connected: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("GET /api/v1/chat/connected"),
        "{request}"
    );
    assert!(request.contains("api-signature:"), "{request}");

    let (url, received) = support::serve(
        400,
        r#"{"error":{"name":"ValidationError","message":"fixture"}}"#,
    )
    .await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.chat_get_connected().await;
    assert!(
        matches!(
            result,
            Err(bitmex_client::OperationError::Rejected { status: 400, .. })
        ),
        "rejection fixture: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("GET /api/v1/chat/connected"),
        "{request}"
    );
}

#[tokio::test]
async fn chat_get_pinned_message_success_and_rejection() {
    let query = ChatGetPinnedMessageQuery::default();
    let (url, received) = support::serve(200, r#"{"id":1,"channelID":1,"messageId":1}"#).await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.chat_get_pinned_message(&query).await;
    assert!(
        result.is_ok(),
        "success fixture for GET /api/v1/chat/pinned: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(request.starts_with("GET /api/v1/chat/pinned"), "{request}");
    assert!(request.contains("api-signature:"), "{request}");

    let (url, received) = support::serve(
        400,
        r#"{"error":{"name":"ValidationError","message":"fixture"}}"#,
    )
    .await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.chat_get_pinned_message(&query).await;
    assert!(
        matches!(
            result,
            Err(bitmex_client::OperationError::Rejected { status: 400, .. })
        ),
        "rejection fixture: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(request.starts_with("GET /api/v1/chat/pinned"), "{request}");
}

#[tokio::test]
async fn get_execution_success_and_rejection() {
    let query = GetExecutionQuery::default();
    let (url, received) = support::serve(200, r#"[]"#).await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.get_execution(&query).await;
    assert!(
        result.is_ok(),
        "success fixture for GET /api/v1/execution: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(request.starts_with("GET /api/v1/execution"), "{request}");
    assert!(request.contains("api-signature:"), "{request}");

    let (url, received) = support::serve(
        400,
        r#"{"error":{"name":"ValidationError","message":"fixture"}}"#,
    )
    .await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.get_execution(&query).await;
    assert!(
        matches!(
            result,
            Err(bitmex_client::OperationError::Rejected { status: 400, .. })
        ),
        "rejection fixture: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(request.starts_with("GET /api/v1/execution"), "{request}");
}

#[tokio::test]
async fn get_execution_trade_history_success_and_rejection() {
    let query = GetExecutionTradeHistoryQuery::default();
    let (url, received) = support::serve(200, r#"[]"#).await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.get_execution_trade_history(&query).await;
    assert!(
        result.is_ok(),
        "success fixture for GET /api/v1/execution/tradeHistory: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("GET /api/v1/execution/tradeHistory"),
        "{request}"
    );
    assert!(request.contains("api-signature:"), "{request}");

    let (url, received) = support::serve(
        400,
        r#"{"error":{"name":"ValidationError","message":"fixture"}}"#,
    )
    .await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.get_execution_trade_history(&query).await;
    assert!(
        matches!(
            result,
            Err(bitmex_client::OperationError::Rejected { status: 400, .. })
        ),
        "rejection fixture: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("GET /api/v1/execution/tradeHistory"),
        "{request}"
    );
}

#[tokio::test]
async fn get_funding_success_and_rejection() {
    let query = GetFundingQuery::default();
    let (url, received) = support::serve(200, r#"[]"#).await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.get_funding(&query).await;
    assert!(
        result.is_ok(),
        "success fixture for GET /api/v1/funding: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(request.starts_with("GET /api/v1/funding"), "{request}");
    assert!(request.contains("api-signature:"), "{request}");

    let (url, received) = support::serve(
        400,
        r#"{"error":{"name":"ValidationError","message":"fixture"}}"#,
    )
    .await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.get_funding(&query).await;
    assert!(
        matches!(
            result,
            Err(bitmex_client::OperationError::Rejected { status: 400, .. })
        ),
        "rejection fixture: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(request.starts_with("GET /api/v1/funding"), "{request}");
}

#[tokio::test]
async fn guild_get_success_and_rejection() {
    let (url, received) = support::serve(200, r#"[]"#).await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.guild_get().await;
    assert!(
        result.is_ok(),
        "success fixture for GET /api/v1/guild: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(request.starts_with("GET /api/v1/guild"), "{request}");
    assert!(request.contains("api-signature:"), "{request}");

    let (url, received) = support::serve(
        400,
        r#"{"error":{"name":"ValidationError","message":"fixture"}}"#,
    )
    .await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.guild_get().await;
    assert!(
        matches!(
            result,
            Err(bitmex_client::OperationError::Rejected { status: 400, .. })
        ),
        "rejection fixture: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(request.starts_with("GET /api/v1/guild"), "{request}");
}

#[tokio::test]
async fn guild_new_success_and_rejection() {
    let (url, received) = support::serve(200, r#"{"created":"fixture","updated":"fixture","name":"fixture","chatChannelId":1,"isPrivate":false}"#).await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.guild_new().await;
    assert!(
        result.is_ok(),
        "success fixture for POST /api/v1/guild: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(request.starts_with("POST /api/v1/guild"), "{request}");
    assert!(request.contains("api-signature:"), "{request}");

    let (url, received) = support::serve(
        400,
        r#"{"error":{"name":"ValidationError","message":"fixture"}}"#,
    )
    .await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.guild_new().await;
    assert!(
        matches!(
            result,
            Err(bitmex_client::OperationError::Rejected { status: 400, .. })
        ),
        "rejection fixture: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(request.starts_with("POST /api/v1/guild"), "{request}");
}

#[tokio::test]
async fn guild_edit_success_and_rejection() {
    let (url, received) = support::serve(200, r#"{"created":"fixture","updated":"fixture","name":"fixture","chatChannelId":1,"isPrivate":false}"#).await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.guild_edit().await;
    assert!(
        result.is_ok(),
        "success fixture for PUT /api/v1/guild: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(request.starts_with("PUT /api/v1/guild"), "{request}");
    assert!(request.contains("api-signature:"), "{request}");

    let (url, received) = support::serve(
        400,
        r#"{"error":{"name":"ValidationError","message":"fixture"}}"#,
    )
    .await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.guild_edit().await;
    assert!(
        matches!(
            result,
            Err(bitmex_client::OperationError::Rejected { status: 400, .. })
        ),
        "rejection fixture: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(request.starts_with("PUT /api/v1/guild"), "{request}");
}

#[tokio::test]
async fn get_instruments_success_and_rejection() {
    let query = GetInstrumentsQuery::default();
    let (url, received) = support::serve(200, r#"[]"#).await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.get_instruments(&query).await;
    assert!(
        result.is_ok(),
        "success fixture for GET /api/v1/instrument: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(request.starts_with("GET /api/v1/instrument"), "{request}");
    assert!(request.contains("api-signature:"), "{request}");

    let (url, received) = support::serve(
        400,
        r#"{"error":{"name":"ValidationError","message":"fixture"}}"#,
    )
    .await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.get_instruments(&query).await;
    assert!(
        matches!(
            result,
            Err(bitmex_client::OperationError::Rejected { status: 400, .. })
        ),
        "rejection fixture: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(request.starts_with("GET /api/v1/instrument"), "{request}");
}

#[tokio::test]
async fn get_active_instruments_success_and_rejection() {
    let (url, received) = support::serve(200, r#"[]"#).await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.get_active_instruments().await;
    assert!(
        result.is_ok(),
        "success fixture for GET /api/v1/instrument/active: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("GET /api/v1/instrument/active"),
        "{request}"
    );
    assert!(request.contains("api-signature:"), "{request}");

    let (url, received) = support::serve(
        400,
        r#"{"error":{"name":"ValidationError","message":"fixture"}}"#,
    )
    .await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.get_active_instruments().await;
    assert!(
        matches!(
            result,
            Err(bitmex_client::OperationError::Rejected { status: 400, .. })
        ),
        "rejection fixture: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("GET /api/v1/instrument/active"),
        "{request}"
    );
}

#[tokio::test]
async fn get_active_and_indices_instruments_success_and_rejection() {
    let (url, received) = support::serve(200, r#"[]"#).await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.get_active_and_indices_instruments().await;
    assert!(
        result.is_ok(),
        "success fixture for GET /api/v1/instrument/activeAndIndices: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("GET /api/v1/instrument/activeAndIndices"),
        "{request}"
    );
    assert!(request.contains("api-signature:"), "{request}");

    let (url, received) = support::serve(
        400,
        r#"{"error":{"name":"ValidationError","message":"fixture"}}"#,
    )
    .await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.get_active_and_indices_instruments().await;
    assert!(
        matches!(
            result,
            Err(bitmex_client::OperationError::Rejected { status: 400, .. })
        ),
        "rejection fixture: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("GET /api/v1/instrument/activeAndIndices"),
        "{request}"
    );
}

#[tokio::test]
async fn get_active_intervals_success_and_rejection() {
    let (url, received) = support::serve(200, r#"[]"#).await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.get_active_intervals().await;
    assert!(
        result.is_ok(),
        "success fixture for GET /api/v1/instrument/activeIntervals: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("GET /api/v1/instrument/activeIntervals"),
        "{request}"
    );
    assert!(request.contains("api-signature:"), "{request}");

    let (url, received) = support::serve(
        400,
        r#"{"error":{"name":"ValidationError","message":"fixture"}}"#,
    )
    .await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.get_active_intervals().await;
    assert!(
        matches!(
            result,
            Err(bitmex_client::OperationError::Rejected { status: 400, .. })
        ),
        "rejection fixture: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("GET /api/v1/instrument/activeIntervals"),
        "{request}"
    );
}

#[tokio::test]
async fn get_composite_index_success_and_rejection() {
    let query = GetCompositeIndexQuery::default();
    let (url, received) = support::serve(200, r#"[]"#).await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.get_composite_index(&query).await;
    assert!(
        result.is_ok(),
        "success fixture for GET /api/v1/instrument/compositeIndex: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("GET /api/v1/instrument/compositeIndex"),
        "{request}"
    );
    assert!(request.contains("api-signature:"), "{request}");

    let (url, received) = support::serve(
        400,
        r#"{"error":{"name":"ValidationError","message":"fixture"}}"#,
    )
    .await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.get_composite_index(&query).await;
    assert!(
        matches!(
            result,
            Err(bitmex_client::OperationError::Rejected { status: 400, .. })
        ),
        "rejection fixture: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("GET /api/v1/instrument/compositeIndex"),
        "{request}"
    );
}

#[tokio::test]
async fn get_indices_instruments_success_and_rejection() {
    let (url, received) = support::serve(200, r#"[]"#).await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.get_indices_instruments().await;
    assert!(
        result.is_ok(),
        "success fixture for GET /api/v1/instrument/indices: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("GET /api/v1/instrument/indices"),
        "{request}"
    );
    assert!(request.contains("api-signature:"), "{request}");

    let (url, received) = support::serve(
        400,
        r#"{"error":{"name":"ValidationError","message":"fixture"}}"#,
    )
    .await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.get_indices_instruments().await;
    assert!(
        matches!(
            result,
            Err(bitmex_client::OperationError::Rejected { status: 400, .. })
        ),
        "rejection fixture: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("GET /api/v1/instrument/indices"),
        "{request}"
    );
}

#[tokio::test]
async fn get_instrument_usd_volume_success_and_rejection() {
    let query = GetInstrumentUsdVolumeQuery::default();
    let (url, received) = support::serve(200, r#"[]"#).await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.get_instrument_usd_volume(&query).await;
    assert!(
        result.is_ok(),
        "success fixture for GET /api/v1/instrument/usdVolume: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("GET /api/v1/instrument/usdVolume"),
        "{request}"
    );
    assert!(request.contains("api-signature:"), "{request}");

    let (url, received) = support::serve(
        400,
        r#"{"error":{"name":"ValidationError","message":"fixture"}}"#,
    )
    .await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.get_instrument_usd_volume(&query).await;
    assert!(
        matches!(
            result,
            Err(bitmex_client::OperationError::Rejected { status: 400, .. })
        ),
        "rejection fixture: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("GET /api/v1/instrument/usdVolume"),
        "{request}"
    );
}

#[tokio::test]
async fn get_insurances_success_and_rejection() {
    let query = GetInsurancesQuery::default();
    let (url, received) = support::serve(200, r#"[]"#).await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.get_insurances(&query).await;
    assert!(
        result.is_ok(),
        "success fixture for GET /api/v1/insurance: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(request.starts_with("GET /api/v1/insurance"), "{request}");
    assert!(request.contains("api-signature:"), "{request}");

    let (url, received) = support::serve(
        400,
        r#"{"error":{"name":"ValidationError","message":"fixture"}}"#,
    )
    .await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.get_insurances(&query).await;
    assert!(
        matches!(
            result,
            Err(bitmex_client::OperationError::Rejected { status: 400, .. })
        ),
        "rejection fixture: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(request.starts_with("GET /api/v1/insurance"), "{request}");
}

#[tokio::test]
async fn leaderboard_get_name_success_and_rejection() {
    let (url, received) = support::serve(200, r#"{}"#).await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.leaderboard_get_name().await;
    assert!(
        result.is_ok(),
        "success fixture for GET /api/v1/leaderboard/name: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("GET /api/v1/leaderboard/name"),
        "{request}"
    );
    assert!(request.contains("api-signature:"), "{request}");

    let (url, received) = support::serve(
        400,
        r#"{"error":{"name":"ValidationError","message":"fixture"}}"#,
    )
    .await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.leaderboard_get_name().await;
    assert!(
        matches!(
            result,
            Err(bitmex_client::OperationError::Rejected { status: 400, .. })
        ),
        "rejection fixture: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("GET /api/v1/leaderboard/name"),
        "{request}"
    );
}

#[tokio::test]
async fn get_liquidation_success_and_rejection() {
    let query = GetLiquidationQuery::default();
    let (url, received) = support::serve(200, r#"[]"#).await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.get_liquidation(&query).await;
    assert!(
        result.is_ok(),
        "success fixture for GET /api/v1/liquidation: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(request.starts_with("GET /api/v1/liquidation"), "{request}");
    assert!(request.contains("api-signature:"), "{request}");

    let (url, received) = support::serve(
        400,
        r#"{"error":{"name":"ValidationError","message":"fixture"}}"#,
    )
    .await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.get_liquidation(&query).await;
    assert!(
        matches!(
            result,
            Err(bitmex_client::OperationError::Rejected { status: 400, .. })
        ),
        "rejection fixture: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(request.starts_with("GET /api/v1/liquidation"), "{request}");
}

#[tokio::test]
async fn managed_sub_account_binding_get_investor_bindings_success_and_rejection() {
    let (url, received) = support::serve(200, r#"[]"#).await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client
        .managed_sub_account_binding_get_investor_bindings()
        .await;
    assert!(
        result.is_ok(),
        "success fixture for GET /api/v1/managedSubAccountBinding/investor: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("GET /api/v1/managedSubAccountBinding/investor"),
        "{request}"
    );
    assert!(request.contains("api-signature:"), "{request}");

    let (url, received) = support::serve(
        400,
        r#"{"error":{"name":"ValidationError","message":"fixture"}}"#,
    )
    .await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client
        .managed_sub_account_binding_get_investor_bindings()
        .await;
    assert!(
        matches!(
            result,
            Err(bitmex_client::OperationError::Rejected { status: 400, .. })
        ),
        "rejection fixture: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("GET /api/v1/managedSubAccountBinding/investor"),
        "{request}"
    );
}

#[tokio::test]
async fn managed_sub_account_binding_get_trading_team_bindings_success_and_rejection() {
    let (url, received) = support::serve(200, r#"[]"#).await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client
        .managed_sub_account_binding_get_trading_team_bindings()
        .await;
    assert!(
        result.is_ok(),
        "success fixture for GET /api/v1/managedSubAccountBinding/tradingTeam: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("GET /api/v1/managedSubAccountBinding/tradingTeam"),
        "{request}"
    );
    assert!(request.contains("api-signature:"), "{request}");

    let (url, received) = support::serve(
        400,
        r#"{"error":{"name":"ValidationError","message":"fixture"}}"#,
    )
    .await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client
        .managed_sub_account_binding_get_trading_team_bindings()
        .await;
    assert!(
        matches!(
            result,
            Err(bitmex_client::OperationError::Rejected { status: 400, .. })
        ),
        "rejection fixture: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("GET /api/v1/managedSubAccountBinding/tradingTeam"),
        "{request}"
    );
}

#[tokio::test]
async fn cancel_order_v1_success_and_rejection() {
    let body: CancelOrderV1Body =
        serde_json::from_str(r#"{"orderID":["fixture"]}"#).expect("body fixture");
    let (url, received) = support::serve(200, r#"[]"#).await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.cancel_order_v1(&body).await;
    assert!(
        result.is_ok(),
        "success fixture for DELETE /api/v1/order: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(request.starts_with("DELETE /api/v1/order"), "{request}");
    assert!(request.contains("api-signature:"), "{request}");

    let (url, received) = support::serve(
        400,
        r#"{"error":{"name":"ValidationError","message":"fixture"}}"#,
    )
    .await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.cancel_order_v1(&body).await;
    assert!(
        matches!(
            result,
            Err(bitmex_client::OperationError::Rejected { status: 400, .. })
        ),
        "rejection fixture: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(request.starts_with("DELETE /api/v1/order"), "{request}");
}

#[tokio::test]
async fn get_order_v1_success_and_rejection() {
    let query = GetOrderV1Query::default();
    let (url, received) = support::serve(200, r#"[]"#).await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.get_order_v1(&query).await;
    assert!(
        result.is_ok(),
        "success fixture for GET /api/v1/order: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(request.starts_with("GET /api/v1/order"), "{request}");
    assert!(request.contains("api-signature:"), "{request}");

    let (url, received) = support::serve(
        400,
        r#"{"error":{"name":"ValidationError","message":"fixture"}}"#,
    )
    .await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.get_order_v1(&query).await;
    assert!(
        matches!(
            result,
            Err(bitmex_client::OperationError::Rejected { status: 400, .. })
        ),
        "rejection fixture: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(request.starts_with("GET /api/v1/order"), "{request}");
}

#[tokio::test]
async fn new_order_v1_success_and_rejection() {
    let body: NewOrderV1Body = serde_json::from_str(
        r#"{"symbol":"fixture","side":"Buy","ordType":"Limit","orderQty":1,"price":1}"#,
    )
    .expect("body fixture");
    let (url, received) = support::serve(200, r#"{"account":1,"cumQty":1,"leavesQty":1,"orderID":"fixture","orderQty":1,"side":"Buy","symbol":"fixture"}"#).await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.new_order_v1(&body).await;
    assert!(
        result.is_ok(),
        "success fixture for POST /api/v1/order: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(request.starts_with("POST /api/v1/order"), "{request}");
    assert!(request.contains("api-signature:"), "{request}");

    let (url, received) = support::serve(
        400,
        r#"{"error":{"name":"ValidationError","message":"fixture"}}"#,
    )
    .await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.new_order_v1(&body).await;
    assert!(
        matches!(
            result,
            Err(bitmex_client::OperationError::Rejected { status: 400, .. })
        ),
        "rejection fixture: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(request.starts_with("POST /api/v1/order"), "{request}");
}

#[tokio::test]
async fn amend_order_v1_success_and_rejection() {
    let body: AmendOrderV1Body =
        serde_json::from_str(r#"{"orderID":"fixture"}"#).expect("body fixture");
    let (url, received) = support::serve(200, r#"{"account":1,"cumQty":1,"leavesQty":1,"orderID":"fixture","orderQty":1,"side":"Buy","symbol":"fixture"}"#).await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.amend_order_v1(&body).await;
    assert!(
        result.is_ok(),
        "success fixture for PUT /api/v1/order: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(request.starts_with("PUT /api/v1/order"), "{request}");
    assert!(request.contains("api-signature:"), "{request}");

    let (url, received) = support::serve(
        400,
        r#"{"error":{"name":"ValidationError","message":"fixture"}}"#,
    )
    .await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.amend_order_v1(&body).await;
    assert!(
        matches!(
            result,
            Err(bitmex_client::OperationError::Rejected { status: 400, .. })
        ),
        "rejection fixture: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(request.starts_with("PUT /api/v1/order"), "{request}");
}

#[tokio::test]
async fn cancel_all_order_v1_success_and_rejection() {
    let body: CancelAllOrderV1Body = serde_json::from_str(r#"{}"#).expect("body fixture");
    let (url, received) = support::serve(200, r#"[]"#).await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.cancel_all_order_v1(&body).await;
    assert!(
        result.is_ok(),
        "success fixture for DELETE /api/v1/order/all: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(request.starts_with("DELETE /api/v1/order/all"), "{request}");
    assert!(request.contains("api-signature:"), "{request}");

    let (url, received) = support::serve(
        400,
        r#"{"error":{"name":"ValidationError","message":"fixture"}}"#,
    )
    .await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.cancel_all_order_v1(&body).await;
    assert!(
        matches!(
            result,
            Err(bitmex_client::OperationError::Rejected { status: 400, .. })
        ),
        "rejection fixture: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(request.starts_with("DELETE /api/v1/order/all"), "{request}");
}

#[tokio::test]
async fn cancel_all_after_order_v1_success_and_rejection() {
    let body: CancelAllAfterOrderV1Body = serde_json::from_str(r#"{}"#).expect("body fixture");
    let (url, received) = support::serve(200, r#"[]"#).await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.cancel_all_after_order_v1(&body).await;
    assert!(
        result.is_ok(),
        "success fixture for POST /api/v1/order/cancelAllAfter: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("POST /api/v1/order/cancelAllAfter"),
        "{request}"
    );
    assert!(request.contains("api-signature:"), "{request}");

    let (url, received) = support::serve(
        400,
        r#"{"error":{"name":"ValidationError","message":"fixture"}}"#,
    )
    .await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.cancel_all_after_order_v1(&body).await;
    assert!(
        matches!(
            result,
            Err(bitmex_client::OperationError::Rejected { status: 400, .. })
        ),
        "rejection fixture: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("POST /api/v1/order/cancelAllAfter"),
        "{request}"
    );
}

#[tokio::test]
async fn get_market_data_v1_success_and_rejection() {
    let (url, received) = support::serve(200, r#"{"id":1,"price":1,"side":"Buy","size":1,"symbol":"fixture","timestamp":"fixture","transactTime":"fixture"}"#).await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.get_market_data_v1().await;
    assert!(
        result.is_ok(),
        "success fixture for GET /api/v1/orderBook/L2: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(request.starts_with("GET /api/v1/orderBook/L2"), "{request}");
    assert!(request.contains("api-signature:"), "{request}");

    let (url, received) = support::serve(
        400,
        r#"{"error":{"name":"ValidationError","message":"fixture"}}"#,
    )
    .await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.get_market_data_v1().await;
    assert!(
        matches!(
            result,
            Err(bitmex_client::OperationError::Rejected { status: 400, .. })
        ),
        "rejection fixture: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(request.starts_with("GET /api/v1/orderBook/L2"), "{request}");
}

#[tokio::test]
async fn porl_get_nonce_success_and_rejection() {
    let (url, received) = support::serve(200, r#"{"account":1,"accountNonce":"fixture","total":1,"balance":1,"height":1,"created":"fixture"}"#).await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.porl_get_nonce().await;
    assert!(
        result.is_ok(),
        "success fixture for GET /api/v1/porl/nonce: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(request.starts_with("GET /api/v1/porl/nonce"), "{request}");
    assert!(request.contains("api-signature:"), "{request}");

    let (url, received) = support::serve(
        400,
        r#"{"error":{"name":"ValidationError","message":"fixture"}}"#,
    )
    .await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.porl_get_nonce().await;
    assert!(
        matches!(
            result,
            Err(bitmex_client::OperationError::Rejected { status: 400, .. })
        ),
        "rejection fixture: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(request.starts_with("GET /api/v1/porl/nonce"), "{request}");
}

#[tokio::test]
async fn porl_get_snapshots_success_and_rejection() {
    let (url, received) = support::serve(200, r#"[]"#).await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.porl_get_snapshots().await;
    assert!(
        result.is_ok(),
        "success fixture for GET /api/v1/porl/snapshots: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("GET /api/v1/porl/snapshots"),
        "{request}"
    );
    assert!(request.contains("api-signature:"), "{request}");

    let (url, received) = support::serve(
        400,
        r#"{"error":{"name":"ValidationError","message":"fixture"}}"#,
    )
    .await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.porl_get_snapshots().await;
    assert!(
        matches!(
            result,
            Err(bitmex_client::OperationError::Rejected { status: 400, .. })
        ),
        "rejection fixture: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("GET /api/v1/porl/snapshots"),
        "{request}"
    );
}

#[tokio::test]
async fn get_position_success_and_rejection() {
    let query = GetPositionQuery::default();
    let (url, received) = support::serve(200, r#"[]"#).await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.get_position(&query).await;
    assert!(
        result.is_ok(),
        "success fixture for GET /api/v1/position: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(request.starts_with("GET /api/v1/position"), "{request}");
    assert!(request.contains("api-signature:"), "{request}");

    let (url, received) = support::serve(
        400,
        r#"{"error":{"name":"ValidationError","message":"fixture"}}"#,
    )
    .await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.get_position(&query).await;
    assert!(
        matches!(
            result,
            Err(bitmex_client::OperationError::Rejected { status: 400, .. })
        ),
        "rejection fixture: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(request.starts_with("GET /api/v1/position"), "{request}");
}

#[tokio::test]
async fn cross_leverage_success_and_rejection() {
    let body: CrossLeverageBody =
        serde_json::from_str(r#"{"leverage":1,"symbol":"fixture"}"#).expect("body fixture");
    let (url, received) = support::serve(200, r#"{}"#).await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.cross_leverage(&body).await;
    assert!(
        result.is_ok(),
        "success fixture for POST /api/v1/position/crossLeverage: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("POST /api/v1/position/crossLeverage"),
        "{request}"
    );
    assert!(request.contains("api-signature:"), "{request}");

    let (url, received) = support::serve(
        400,
        r#"{"error":{"name":"ValidationError","message":"fixture"}}"#,
    )
    .await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.cross_leverage(&body).await;
    assert!(
        matches!(
            result,
            Err(bitmex_client::OperationError::Rejected { status: 400, .. })
        ),
        "rejection fixture: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("POST /api/v1/position/crossLeverage"),
        "{request}"
    );
}

#[tokio::test]
async fn isolate_success_and_rejection() {
    let body: IsolateBody = serde_json::from_str(r#"{"symbol":"fixture"}"#).expect("body fixture");
    let (url, received) = support::serve(200, r#"{}"#).await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.isolate(&body).await;
    assert!(
        result.is_ok(),
        "success fixture for POST /api/v1/position/isolate: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("POST /api/v1/position/isolate"),
        "{request}"
    );
    assert!(request.contains("api-signature:"), "{request}");

    let (url, received) = support::serve(
        400,
        r#"{"error":{"name":"ValidationError","message":"fixture"}}"#,
    )
    .await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.isolate(&body).await;
    assert!(
        matches!(
            result,
            Err(bitmex_client::OperationError::Rejected { status: 400, .. })
        ),
        "rejection fixture: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("POST /api/v1/position/isolate"),
        "{request}"
    );
}

#[tokio::test]
async fn leverage_success_and_rejection() {
    let body: LeverageBody =
        serde_json::from_str(r#"{"leverage":1,"symbol":"fixture"}"#).expect("body fixture");
    let (url, received) = support::serve(200, r#"{}"#).await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.leverage(&body).await;
    assert!(
        result.is_ok(),
        "success fixture for POST /api/v1/position/leverage: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("POST /api/v1/position/leverage"),
        "{request}"
    );
    assert!(request.contains("api-signature:"), "{request}");

    let (url, received) = support::serve(
        400,
        r#"{"error":{"name":"ValidationError","message":"fixture"}}"#,
    )
    .await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.leverage(&body).await;
    assert!(
        matches!(
            result,
            Err(bitmex_client::OperationError::Rejected { status: 400, .. })
        ),
        "rejection fixture: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("POST /api/v1/position/leverage"),
        "{request}"
    );
}

#[tokio::test]
async fn risk_limit_success_and_rejection() {
    let body: RiskLimitBody =
        serde_json::from_str(r#"{"riskLimit":1,"symbol":"fixture"}"#).expect("body fixture");
    let (url, received) = support::serve(200, r#"{}"#).await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.risk_limit(&body).await;
    assert!(
        result.is_ok(),
        "success fixture for POST /api/v1/position/riskLimit: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("POST /api/v1/position/riskLimit"),
        "{request}"
    );
    assert!(request.contains("api-signature:"), "{request}");

    let (url, received) = support::serve(
        400,
        r#"{"error":{"name":"ValidationError","message":"fixture"}}"#,
    )
    .await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.risk_limit(&body).await;
    assert!(
        matches!(
            result,
            Err(bitmex_client::OperationError::Rejected { status: 400, .. })
        ),
        "rejection fixture: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("POST /api/v1/position/riskLimit"),
        "{request}"
    );
}

#[tokio::test]
async fn transfer_margin_success_and_rejection() {
    let body: TransferMarginBody =
        serde_json::from_str(r#"{"amount":1,"symbol":"fixture"}"#).expect("body fixture");
    let (url, received) = support::serve(200, r#"{}"#).await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.transfer_margin(&body).await;
    assert!(
        result.is_ok(),
        "success fixture for POST /api/v1/position/transferMargin: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("POST /api/v1/position/transferMargin"),
        "{request}"
    );
    assert!(request.contains("api-signature:"), "{request}");

    let (url, received) = support::serve(
        400,
        r#"{"error":{"name":"ValidationError","message":"fixture"}}"#,
    )
    .await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.transfer_margin(&body).await;
    assert!(
        matches!(
            result,
            Err(bitmex_client::OperationError::Rejected { status: 400, .. })
        ),
        "rejection fixture: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("POST /api/v1/position/transferMargin"),
        "{request}"
    );
}

#[tokio::test]
async fn get_position_history_success_and_rejection() {
    let query = GetPositionHistoryQuery::default();
    let (url, received) = support::serve(200, r#"[]"#).await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.get_position_history(&query).await;
    assert!(
        result.is_ok(),
        "success fixture for GET /api/v1/positionHistory: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("GET /api/v1/positionHistory"),
        "{request}"
    );
    assert!(request.contains("api-signature:"), "{request}");

    let (url, received) = support::serve(
        400,
        r#"{"error":{"name":"ValidationError","message":"fixture"}}"#,
    )
    .await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.get_position_history(&query).await;
    assert!(
        matches!(
            result,
            Err(bitmex_client::OperationError::Rejected { status: 400, .. })
        ),
        "rejection fixture: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("GET /api/v1/positionHistory"),
        "{request}"
    );
}

#[tokio::test]
async fn get_quote_success_and_rejection() {
    let query = GetQuoteQuery::default();
    let (url, received) = support::serve(200, r#"[]"#).await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.get_quote(&query).await;
    assert!(
        result.is_ok(),
        "success fixture for GET /api/v1/quote: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(request.starts_with("GET /api/v1/quote"), "{request}");
    assert!(request.contains("api-signature:"), "{request}");

    let (url, received) = support::serve(
        400,
        r#"{"error":{"name":"ValidationError","message":"fixture"}}"#,
    )
    .await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.get_quote(&query).await;
    assert!(
        matches!(
            result,
            Err(bitmex_client::OperationError::Rejected { status: 400, .. })
        ),
        "rejection fixture: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(request.starts_with("GET /api/v1/quote"), "{request}");
}

#[tokio::test]
async fn get_quote_bucketed_success_and_rejection() {
    let query = GetQuoteBucketedQuery::default();
    let (url, received) = support::serve(200, r#"[]"#).await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.get_quote_bucketed(&query).await;
    assert!(
        result.is_ok(),
        "success fixture for GET /api/v1/quote/bucketed: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("GET /api/v1/quote/bucketed"),
        "{request}"
    );
    assert!(request.contains("api-signature:"), "{request}");

    let (url, received) = support::serve(
        400,
        r#"{"error":{"name":"ValidationError","message":"fixture"}}"#,
    )
    .await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.get_quote_bucketed(&query).await;
    assert!(
        matches!(
            result,
            Err(bitmex_client::OperationError::Rejected { status: 400, .. })
        ),
        "rejection fixture: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("GET /api/v1/quote/bucketed"),
        "{request}"
    );
}

#[tokio::test]
async fn referral_code_get_all_codes_for_user_success_and_rejection() {
    let (url, received) = support::serve(200, r#"[]"#).await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.referral_code_get_all_codes_for_user().await;
    assert!(
        result.is_ok(),
        "success fixture for GET /api/v1/referralCode: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(request.starts_with("GET /api/v1/referralCode"), "{request}");
    assert!(request.contains("api-signature:"), "{request}");

    let (url, received) = support::serve(
        400,
        r#"{"error":{"name":"ValidationError","message":"fixture"}}"#,
    )
    .await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.referral_code_get_all_codes_for_user().await;
    assert!(
        matches!(
            result,
            Err(bitmex_client::OperationError::Rejected { status: 400, .. })
        ),
        "rejection fixture: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(request.starts_with("GET /api/v1/referralCode"), "{request}");
}

#[tokio::test]
async fn referral_code_delete_referral_code_success_and_rejection() {
    let id = PathId::new("fixture").expect("path id");
    let (url, received) = support::serve(200, r#"false"#).await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.referral_code_delete_referral_code(&id).await;
    assert!(
        result.is_ok(),
        "success fixture for DELETE /api/v1/referralCode/fixture: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("DELETE /api/v1/referralCode/fixture"),
        "{request}"
    );
    assert!(request.contains("api-signature:"), "{request}");

    let (url, received) = support::serve(
        400,
        r#"{"error":{"name":"ValidationError","message":"fixture"}}"#,
    )
    .await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.referral_code_delete_referral_code(&id).await;
    assert!(
        matches!(
            result,
            Err(bitmex_client::OperationError::Rejected { status: 400, .. })
        ),
        "rejection fixture: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("DELETE /api/v1/referralCode/fixture"),
        "{request}"
    );
}

#[tokio::test]
async fn referral_code_check_referral_code_success_and_rejection() {
    let code = PathId::new("fixture").expect("path id");
    let (url, received) = support::serve(200, r#"{}"#).await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.referral_code_check_referral_code(&code).await;
    assert!(
        result.is_ok(),
        "success fixture for GET /api/v1/referralCode/check/fixture: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("GET /api/v1/referralCode/check/fixture"),
        "{request}"
    );
    assert!(request.contains("api-signature:"), "{request}");

    let (url, received) = support::serve(
        400,
        r#"{"error":{"name":"ValidationError","message":"fixture"}}"#,
    )
    .await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.referral_code_check_referral_code(&code).await;
    assert!(
        matches!(
            result,
            Err(bitmex_client::OperationError::Rejected { status: 400, .. })
        ),
        "rejection fixture: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("GET /api/v1/referralCode/check/fixture"),
        "{request}"
    );
}

#[tokio::test]
async fn get_settlements_success_and_rejection() {
    let query = GetSettlementsQuery::default();
    let (url, received) = support::serve(200, r#"[]"#).await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.get_settlements(&query).await;
    assert!(
        result.is_ok(),
        "success fixture for GET /api/v1/settlement: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(request.starts_with("GET /api/v1/settlement"), "{request}");
    assert!(request.contains("api-signature:"), "{request}");

    let (url, received) = support::serve(
        400,
        r#"{"error":{"name":"ValidationError","message":"fixture"}}"#,
    )
    .await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.get_settlements(&query).await;
    assert!(
        matches!(
            result,
            Err(bitmex_client::OperationError::Rejected { status: 400, .. })
        ),
        "rejection fixture: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(request.starts_with("GET /api/v1/settlement"), "{request}");
}

#[tokio::test]
async fn get_stats_success_and_rejection() {
    let (url, received) = support::serve(200, r#"[]"#).await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.get_stats().await;
    assert!(
        result.is_ok(),
        "success fixture for GET /api/v1/stats: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(request.starts_with("GET /api/v1/stats"), "{request}");
    assert!(request.contains("api-signature:"), "{request}");

    let (url, received) = support::serve(
        400,
        r#"{"error":{"name":"ValidationError","message":"fixture"}}"#,
    )
    .await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.get_stats().await;
    assert!(
        matches!(
            result,
            Err(bitmex_client::OperationError::Rejected { status: 400, .. })
        ),
        "rejection fixture: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(request.starts_with("GET /api/v1/stats"), "{request}");
}

#[tokio::test]
async fn get_stats_history_success_and_rejection() {
    let (url, received) = support::serve(200, r#"[]"#).await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.get_stats_history().await;
    assert!(
        result.is_ok(),
        "success fixture for GET /api/v1/stats/history: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("GET /api/v1/stats/history"),
        "{request}"
    );
    assert!(request.contains("api-signature:"), "{request}");

    let (url, received) = support::serve(
        400,
        r#"{"error":{"name":"ValidationError","message":"fixture"}}"#,
    )
    .await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.get_stats_history().await;
    assert!(
        matches!(
            result,
            Err(bitmex_client::OperationError::Rejected { status: 400, .. })
        ),
        "rejection fixture: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("GET /api/v1/stats/history"),
        "{request}"
    );
}

#[tokio::test]
async fn get_stats_history_usd_success_and_rejection() {
    let (url, received) = support::serve(200, r#"[]"#).await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.get_stats_history_usd().await;
    assert!(
        result.is_ok(),
        "success fixture for GET /api/v1/stats/historyUSD: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("GET /api/v1/stats/historyUSD"),
        "{request}"
    );
    assert!(request.contains("api-signature:"), "{request}");

    let (url, received) = support::serve(
        400,
        r#"{"error":{"name":"ValidationError","message":"fixture"}}"#,
    )
    .await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.get_stats_history_usd().await;
    assert!(
        matches!(
            result,
            Err(bitmex_client::OperationError::Rejected { status: 400, .. })
        ),
        "rejection fixture: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("GET /api/v1/stats/historyUSD"),
        "{request}"
    );
}

#[tokio::test]
async fn get_trade_success_and_rejection() {
    let query = GetTradeQuery::default();
    let (url, received) = support::serve(200, r#"[]"#).await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.get_trade(&query).await;
    assert!(
        result.is_ok(),
        "success fixture for GET /api/v1/trade: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(request.starts_with("GET /api/v1/trade"), "{request}");
    assert!(request.contains("api-signature:"), "{request}");

    let (url, received) = support::serve(
        400,
        r#"{"error":{"name":"ValidationError","message":"fixture"}}"#,
    )
    .await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.get_trade(&query).await;
    assert!(
        matches!(
            result,
            Err(bitmex_client::OperationError::Rejected { status: 400, .. })
        ),
        "rejection fixture: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(request.starts_with("GET /api/v1/trade"), "{request}");
}

#[tokio::test]
async fn get_trade_bucketed_success_and_rejection() {
    let query = GetTradeBucketedQuery::default();
    let (url, received) = support::serve(200, r#"[]"#).await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.get_trade_bucketed(&query).await;
    assert!(
        result.is_ok(),
        "success fixture for GET /api/v1/trade/bucketed: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("GET /api/v1/trade/bucketed"),
        "{request}"
    );
    assert!(request.contains("api-signature:"), "{request}");

    let (url, received) = support::serve(
        400,
        r#"{"error":{"name":"ValidationError","message":"fixture"}}"#,
    )
    .await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.get_trade_bucketed(&query).await;
    assert!(
        matches!(
            result,
            Err(bitmex_client::OperationError::Rejected { status: 400, .. })
        ),
        "rejection fixture: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("GET /api/v1/trade/bucketed"),
        "{request}"
    );
}

#[tokio::test]
async fn user_get_success_and_rejection() {
    let (url, received) = support::serve(200, r#"{"username":"fixture","isUser":false}"#).await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.user_get().await;
    assert!(
        result.is_ok(),
        "success fixture for GET /api/v1/user: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(request.starts_with("GET /api/v1/user"), "{request}");
    assert!(request.contains("api-signature:"), "{request}");

    let (url, received) = support::serve(
        400,
        r#"{"error":{"name":"ValidationError","message":"fixture"}}"#,
    )
    .await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.user_get().await;
    assert!(
        matches!(
            result,
            Err(bitmex_client::OperationError::Rejected { status: 400, .. })
        ),
        "rejection fixture: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(request.starts_with("GET /api/v1/user"), "{request}");
}

#[tokio::test]
async fn get_affiliate_status_success_and_rejection() {
    let query = GetAffiliateStatusQuery::default();
    let (url, received) = support::serve(200, r#"[]"#).await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.get_affiliate_status(&query).await;
    assert!(
        result.is_ok(),
        "success fixture for GET /api/v1/user/affiliateStatus: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("GET /api/v1/user/affiliateStatus"),
        "{request}"
    );
    assert!(request.contains("api-signature:"), "{request}");

    let (url, received) = support::serve(
        400,
        r#"{"error":{"name":"ValidationError","message":"fixture"}}"#,
    )
    .await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.get_affiliate_status(&query).await;
    assert!(
        matches!(
            result,
            Err(bitmex_client::OperationError::Rejected { status: 400, .. })
        ),
        "rejection fixture: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("GET /api/v1/user/affiliateStatus"),
        "{request}"
    );
}

#[tokio::test]
async fn user_cancel_withdrawal_success_and_rejection() {
    let (url, received) = support::serve(200, r#"{"transactID":"fixture"}"#).await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.user_cancel_withdrawal().await;
    assert!(
        result.is_ok(),
        "success fixture for POST /api/v1/user/cancelWithdrawal: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("POST /api/v1/user/cancelWithdrawal"),
        "{request}"
    );
    assert!(request.contains("api-signature:"), "{request}");

    let (url, received) = support::serve(
        400,
        r#"{"error":{"name":"ValidationError","message":"fixture"}}"#,
    )
    .await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.user_cancel_withdrawal().await;
    assert!(
        matches!(
            result,
            Err(bitmex_client::OperationError::Rejected { status: 400, .. })
        ),
        "rejection fixture: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("POST /api/v1/user/cancelWithdrawal"),
        "{request}"
    );
}

#[tokio::test]
async fn get_user_commission_success_and_rejection() {
    let (url, received) = support::serve(200, r#"[]"#).await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.get_user_commission().await;
    assert!(
        result.is_ok(),
        "success fixture for GET /api/v1/user/commission: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("GET /api/v1/user/commission"),
        "{request}"
    );
    assert!(request.contains("api-signature:"), "{request}");

    let (url, received) = support::serve(
        400,
        r#"{"error":{"name":"ValidationError","message":"fixture"}}"#,
    )
    .await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.get_user_commission().await;
    assert!(
        matches!(
            result,
            Err(bitmex_client::OperationError::Rejected { status: 400, .. })
        ),
        "rejection fixture: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("GET /api/v1/user/commission"),
        "{request}"
    );
}

#[tokio::test]
async fn user_communication_token_success_and_rejection() {
    let (url, received) = support::serve(200, r#"false"#).await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.user_communication_token().await;
    assert!(
        result.is_ok(),
        "success fixture for POST /api/v1/user/communicationToken: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("POST /api/v1/user/communicationToken"),
        "{request}"
    );
    assert!(request.contains("api-signature:"), "{request}");

    let (url, received) = support::serve(
        400,
        r#"{"error":{"name":"ValidationError","message":"fixture"}}"#,
    )
    .await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.user_communication_token().await;
    assert!(
        matches!(
            result,
            Err(bitmex_client::OperationError::Rejected { status: 400, .. })
        ),
        "rejection fixture: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("POST /api/v1/user/communicationToken"),
        "{request}"
    );
}

#[tokio::test]
async fn user_confirm_success_and_rejection() {
    let (url, received) = support::serve(200, r#"{"updated":"fixture","id":"fixture"}"#).await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.user_confirm().await;
    assert!(
        result.is_ok(),
        "success fixture for POST /api/v1/user/confirmEmail: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("POST /api/v1/user/confirmEmail"),
        "{request}"
    );
    assert!(request.contains("api-signature:"), "{request}");

    let (url, received) = support::serve(
        400,
        r#"{"error":{"name":"ValidationError","message":"fixture"}}"#,
    )
    .await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.user_confirm().await;
    assert!(
        matches!(
            result,
            Err(bitmex_client::OperationError::Rejected { status: 400, .. })
        ),
        "rejection fixture: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("POST /api/v1/user/confirmEmail"),
        "{request}"
    );
}

#[tokio::test]
async fn user_confirm_withdrawal_success_and_rejection() {
    let (url, received) = support::serve(200, r#"{"transactID":"fixture"}"#).await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.user_confirm_withdrawal().await;
    assert!(
        result.is_ok(),
        "success fixture for POST /api/v1/user/confirmWithdrawal: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("POST /api/v1/user/confirmWithdrawal"),
        "{request}"
    );
    assert!(request.contains("api-signature:"), "{request}");

    let (url, received) = support::serve(
        400,
        r#"{"error":{"name":"ValidationError","message":"fixture"}}"#,
    )
    .await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.user_confirm_withdrawal().await;
    assert!(
        matches!(
            result,
            Err(bitmex_client::OperationError::Rejected { status: 400, .. })
        ),
        "rejection fixture: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("POST /api/v1/user/confirmWithdrawal"),
        "{request}"
    );
}

#[tokio::test]
async fn get_user_csa_success_and_rejection() {
    let (url, received) = support::serve(200, r#"[]"#).await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.get_user_csa().await;
    assert!(
        result.is_ok(),
        "success fixture for GET /api/v1/user/csa: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(request.starts_with("GET /api/v1/user/csa"), "{request}");
    assert!(request.contains("api-signature:"), "{request}");

    let (url, received) = support::serve(
        400,
        r#"{"error":{"name":"ValidationError","message":"fixture"}}"#,
    )
    .await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.get_user_csa().await;
    assert!(
        matches!(
            result,
            Err(bitmex_client::OperationError::Rejected { status: 400, .. })
        ),
        "rejection fixture: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(request.starts_with("GET /api/v1/user/csa"), "{request}");
}

#[tokio::test]
async fn user_get_deposit_address_success_and_rejection() {
    let query = UserGetDepositAddressQuery::default();
    let (url, received) = support::serve(200, r#""fixture""#).await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.user_get_deposit_address(&query).await;
    assert!(
        result.is_ok(),
        "success fixture for GET /api/v1/user/depositAddress: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("GET /api/v1/user/depositAddress"),
        "{request}"
    );
    assert!(request.contains("api-signature:"), "{request}");

    let (url, received) = support::serve(
        400,
        r#"{"error":{"name":"ValidationError","message":"fixture"}}"#,
    )
    .await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.user_get_deposit_address(&query).await;
    assert!(
        matches!(
            result,
            Err(bitmex_client::OperationError::Rejected { status: 400, .. })
        ),
        "rejection fixture: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("GET /api/v1/user/depositAddress"),
        "{request}"
    );
}

#[tokio::test]
async fn user_get_deposit_address_information_success_and_rejection() {
    let query = UserGetDepositAddressInformationQuery::default();
    let (url, received) = support::serve(200, r#"{"address":"fixture"}"#).await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.user_get_deposit_address_information(&query).await;
    assert!(
        result.is_ok(),
        "success fixture for GET /api/v1/user/depositAddressInformation: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("GET /api/v1/user/depositAddressInformation"),
        "{request}"
    );
    assert!(request.contains("api-signature:"), "{request}");

    let (url, received) = support::serve(
        400,
        r#"{"error":{"name":"ValidationError","message":"fixture"}}"#,
    )
    .await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.user_get_deposit_address_information(&query).await;
    assert!(
        matches!(
            result,
            Err(bitmex_client::OperationError::Rejected { status: 400, .. })
        ),
        "rejection fixture: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("GET /api/v1/user/depositAddressInformation"),
        "{request}"
    );
}

#[tokio::test]
async fn get_execution_history_success_and_rejection() {
    let query = GetExecutionHistoryQuery::default();
    let (url, received) = support::serve(200, r#"{"account":1,"cumQty":1,"execID":"fixture","execType":"New","leavesQty":1,"orderID":"fixture","orderQty":1,"side":"Buy","symbol":"fixture"}"#).await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.get_execution_history(&query).await;
    assert!(
        result.is_ok(),
        "success fixture for GET /api/v1/user/executionHistory: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("GET /api/v1/user/executionHistory"),
        "{request}"
    );
    assert!(request.contains("api-signature:"), "{request}");

    let (url, received) = support::serve(
        400,
        r#"{"error":{"name":"ValidationError","message":"fixture"}}"#,
    )
    .await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.get_execution_history(&query).await;
    assert!(
        matches!(
            result,
            Err(bitmex_client::OperationError::Rejected { status: 400, .. })
        ),
        "rejection fixture: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("GET /api/v1/user/executionHistory"),
        "{request}"
    );
}

#[tokio::test]
async fn user_get_wallet_transfer_accounts_success_and_rejection() {
    let (url, received) = support::serve(200, r#"[]"#).await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.user_get_wallet_transfer_accounts().await;
    assert!(
        result.is_ok(),
        "success fixture for GET /api/v1/user/getWalletTransferAccounts: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("GET /api/v1/user/getWalletTransferAccounts"),
        "{request}"
    );
    assert!(request.contains("api-signature:"), "{request}");

    let (url, received) = support::serve(
        400,
        r#"{"error":{"name":"ValidationError","message":"fixture"}}"#,
    )
    .await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.user_get_wallet_transfer_accounts().await;
    assert!(
        matches!(
            result,
            Err(bitmex_client::OperationError::Rejected { status: 400, .. })
        ),
        "rejection fixture: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("GET /api/v1/user/getWalletTransferAccounts"),
        "{request}"
    );
}

#[tokio::test]
async fn user_logout_success_and_rejection() {
    let (url, received) = support::serve(200, r#"null"#).await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.user_logout().await;
    assert!(
        result.is_ok(),
        "success fixture for POST /api/v1/user/logout: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(request.starts_with("POST /api/v1/user/logout"), "{request}");
    assert!(request.contains("api-signature:"), "{request}");

    let (url, received) = support::serve(
        400,
        r#"{"error":{"name":"ValidationError","message":"fixture"}}"#,
    )
    .await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.user_logout().await;
    assert!(
        matches!(
            result,
            Err(bitmex_client::OperationError::Rejected { status: 400, .. })
        ),
        "rejection fixture: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(request.starts_with("POST /api/v1/user/logout"), "{request}");
}

#[tokio::test]
async fn get_margin_success_and_rejection() {
    let query = GetMarginQuery::default();
    let (url, received) = support::serve(200, r#"[]"#).await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.get_margin(&query).await;
    assert!(
        result.is_ok(),
        "success fixture for GET /api/v1/user/margin: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(request.starts_with("GET /api/v1/user/margin"), "{request}");
    assert!(request.contains("api-signature:"), "{request}");

    let (url, received) = support::serve(
        400,
        r#"{"error":{"name":"ValidationError","message":"fixture"}}"#,
    )
    .await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.get_margin(&query).await;
    assert!(
        matches!(
            result,
            Err(bitmex_client::OperationError::Rejected { status: 400, .. })
        ),
        "rejection fixture: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(request.starts_with("GET /api/v1/user/margin"), "{request}");
}

#[tokio::test]
async fn get_margining_mode_success_and_rejection() {
    let query = GetMarginingModeQuery::default();
    let (url, received) = support::serve(200, r#"[]"#).await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.get_margining_mode(&query).await;
    assert!(
        result.is_ok(),
        "success fixture for GET /api/v1/user/marginingMode: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("GET /api/v1/user/marginingMode"),
        "{request}"
    );
    assert!(request.contains("api-signature:"), "{request}");

    let (url, received) = support::serve(
        400,
        r#"{"error":{"name":"ValidationError","message":"fixture"}}"#,
    )
    .await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.get_margining_mode(&query).await;
    assert!(
        matches!(
            result,
            Err(bitmex_client::OperationError::Rejected { status: 400, .. })
        ),
        "rejection fixture: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("GET /api/v1/user/marginingMode"),
        "{request}"
    );
}

#[tokio::test]
async fn margining_mode_success_and_rejection() {
    let body: MarginingModeBody = serde_json::from_str(r#"{}"#).expect("body fixture");
    let (url, received) = support::serve(200, r#"{"account":1}"#).await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.margining_mode(&body).await;
    assert!(
        result.is_ok(),
        "success fixture for POST /api/v1/user/marginingMode: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("POST /api/v1/user/marginingMode"),
        "{request}"
    );
    assert!(request.contains("api-signature:"), "{request}");

    let (url, received) = support::serve(
        400,
        r#"{"error":{"name":"ValidationError","message":"fixture"}}"#,
    )
    .await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.margining_mode(&body).await;
    assert!(
        matches!(
            result,
            Err(bitmex_client::OperationError::Rejected { status: 400, .. })
        ),
        "rejection fixture: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("POST /api/v1/user/marginingMode"),
        "{request}"
    );
}

#[tokio::test]
async fn position_mode_success_and_rejection() {
    let body: PositionModeBody = serde_json::from_str(r#"{}"#).expect("body fixture");
    let (url, received) = support::serve(200, r#"{"account":1}"#).await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.position_mode(&body).await;
    assert!(
        result.is_ok(),
        "success fixture for POST /api/v1/user/positionMode: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("POST /api/v1/user/positionMode"),
        "{request}"
    );
    assert!(request.contains("api-signature:"), "{request}");

    let (url, received) = support::serve(
        400,
        r#"{"error":{"name":"ValidationError","message":"fixture"}}"#,
    )
    .await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.position_mode(&body).await;
    assert!(
        matches!(
            result,
            Err(bitmex_client::OperationError::Rejected { status: 400, .. })
        ),
        "rejection fixture: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("POST /api/v1/user/positionMode"),
        "{request}"
    );
}

#[tokio::test]
async fn user_save_preferences_success_and_rejection() {
    let (url, received) = support::serve(200, r#"{"username":"fixture","isUser":false}"#).await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.user_save_preferences().await;
    assert!(
        result.is_ok(),
        "success fixture for POST /api/v1/user/preferences: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("POST /api/v1/user/preferences"),
        "{request}"
    );
    assert!(request.contains("api-signature:"), "{request}");

    let (url, received) = support::serve(
        400,
        r#"{"error":{"name":"ValidationError","message":"fixture"}}"#,
    )
    .await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.user_save_preferences().await;
    assert!(
        matches!(
            result,
            Err(bitmex_client::OperationError::Rejected { status: 400, .. })
        ),
        "rejection fixture: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("POST /api/v1/user/preferences"),
        "{request}"
    );
}

#[tokio::test]
async fn get_quote_fill_ratio_success_and_rejection() {
    let query = GetQuoteFillRatioQuery::default();
    let (url, received) = support::serve(200, r#"[]"#).await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.get_quote_fill_ratio(&query).await;
    assert!(
        result.is_ok(),
        "success fixture for GET /api/v1/user/quoteFillRatio: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("GET /api/v1/user/quoteFillRatio"),
        "{request}"
    );
    assert!(request.contains("api-signature:"), "{request}");

    let (url, received) = support::serve(
        400,
        r#"{"error":{"name":"ValidationError","message":"fixture"}}"#,
    )
    .await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.get_quote_fill_ratio(&query).await;
    assert!(
        matches!(
            result,
            Err(bitmex_client::OperationError::Rejected { status: 400, .. })
        ),
        "rejection fixture: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("GET /api/v1/user/quoteFillRatio"),
        "{request}"
    );
}

#[tokio::test]
async fn user_get_quote_value_ratio_success_and_rejection() {
    let query = UserGetQuoteValueRatioQuery::default();
    let (url, received) = support::serve(200, r#"{}"#).await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.user_get_quote_value_ratio(&query).await;
    assert!(
        result.is_ok(),
        "success fixture for GET /api/v1/user/quoteValueRatio: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("GET /api/v1/user/quoteValueRatio"),
        "{request}"
    );
    assert!(request.contains("api-signature:"), "{request}");

    let (url, received) = support::serve(
        400,
        r#"{"error":{"name":"ValidationError","message":"fixture"}}"#,
    )
    .await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.user_get_quote_value_ratio(&query).await;
    assert!(
        matches!(
            result,
            Err(bitmex_client::OperationError::Rejected { status: 400, .. })
        ),
        "rejection fixture: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("GET /api/v1/user/quoteValueRatio"),
        "{request}"
    );
}

#[tokio::test]
async fn user_request_withdrawal_success_and_rejection() {
    let (url, received) = support::serve(200, r#"{"transactID":"fixture"}"#).await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.user_request_withdrawal().await;
    assert!(
        result.is_ok(),
        "success fixture for POST /api/v1/user/requestWithdrawal: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("POST /api/v1/user/requestWithdrawal"),
        "{request}"
    );
    assert!(request.contains("api-signature:"), "{request}");

    let (url, received) = support::serve(
        400,
        r#"{"error":{"name":"ValidationError","message":"fixture"}}"#,
    )
    .await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.user_request_withdrawal().await;
    assert!(
        matches!(
            result,
            Err(bitmex_client::OperationError::Rejected { status: 400, .. })
        ),
        "rejection fixture: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("POST /api/v1/user/requestWithdrawal"),
        "{request}"
    );
}

#[tokio::test]
async fn get_staked_amount_success_and_rejection() {
    let query = GetStakedAmountQuery::default();
    let (url, received) = support::serve(200, r#"[]"#).await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.get_staked_amount(&query).await;
    assert!(
        result.is_ok(),
        "success fixture for GET /api/v1/user/staking: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(request.starts_with("GET /api/v1/user/staking"), "{request}");
    assert!(request.contains("api-signature:"), "{request}");

    let (url, received) = support::serve(
        400,
        r#"{"error":{"name":"ValidationError","message":"fixture"}}"#,
    )
    .await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.get_staked_amount(&query).await;
    assert!(
        matches!(
            result,
            Err(bitmex_client::OperationError::Rejected { status: 400, .. })
        ),
        "rejection fixture: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(request.starts_with("GET /api/v1/user/staking"), "{request}");
}

#[tokio::test]
async fn get_staking_instruments_success_and_rejection() {
    let query = GetStakingInstrumentsQuery::default();
    let (url, received) = support::serve(200, r#"[]"#).await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.get_staking_instruments(&query).await;
    assert!(
        result.is_ok(),
        "success fixture for GET /api/v1/user/staking/instruments: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("GET /api/v1/user/staking/instruments"),
        "{request}"
    );
    assert!(request.contains("api-signature:"), "{request}");

    let (url, received) = support::serve(
        400,
        r#"{"error":{"name":"ValidationError","message":"fixture"}}"#,
    )
    .await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.get_staking_instruments(&query).await;
    assert!(
        matches!(
            result,
            Err(bitmex_client::OperationError::Rejected { status: 400, .. })
        ),
        "rejection fixture: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("GET /api/v1/user/staking/instruments"),
        "{request}"
    );
}

#[tokio::test]
async fn get_trading_settings_success_and_rejection() {
    let query = GetTradingSettingsQuery::default();
    let (url, received) = support::serve(200, r#"[]"#).await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.get_trading_settings(&query).await;
    assert!(
        result.is_ok(),
        "success fixture for GET /api/v1/user/tradingSettings: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("GET /api/v1/user/tradingSettings"),
        "{request}"
    );
    assert!(request.contains("api-signature:"), "{request}");

    let (url, received) = support::serve(
        400,
        r#"{"error":{"name":"ValidationError","message":"fixture"}}"#,
    )
    .await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.get_trading_settings(&query).await;
    assert!(
        matches!(
            result,
            Err(bitmex_client::OperationError::Rejected { status: 400, .. })
        ),
        "rejection fixture: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("GET /api/v1/user/tradingSettings"),
        "{request}"
    );
}

#[tokio::test]
async fn get_trading_volume_success_and_rejection() {
    let (url, received) = support::serve(200, r#"{}"#).await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.get_trading_volume().await;
    assert!(
        result.is_ok(),
        "success fixture for GET /api/v1/user/tradingVolume: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("GET /api/v1/user/tradingVolume"),
        "{request}"
    );
    assert!(request.contains("api-signature:"), "{request}");

    let (url, received) = support::serve(
        400,
        r#"{"error":{"name":"ValidationError","message":"fixture"}}"#,
    )
    .await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.get_trading_volume().await;
    assert!(
        matches!(
            result,
            Err(bitmex_client::OperationError::Rejected { status: 400, .. })
        ),
        "rejection fixture: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("GET /api/v1/user/tradingVolume"),
        "{request}"
    );
}

#[tokio::test]
async fn get_unstaking_requests_success_and_rejection() {
    let query = GetUnstakingRequestsQuery::default();
    let (url, received) = support::serve(200, r#"[]"#).await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.get_unstaking_requests(&query).await;
    assert!(
        result.is_ok(),
        "success fixture for GET /api/v1/user/unstakingRequests: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("GET /api/v1/user/unstakingRequests"),
        "{request}"
    );
    assert!(request.contains("api-signature:"), "{request}");

    let (url, received) = support::serve(
        400,
        r#"{"error":{"name":"ValidationError","message":"fixture"}}"#,
    )
    .await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.get_unstaking_requests(&query).await;
    assert!(
        matches!(
            result,
            Err(bitmex_client::OperationError::Rejected { status: 400, .. })
        ),
        "rejection fixture: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("GET /api/v1/user/unstakingRequests"),
        "{request}"
    );
}

#[tokio::test]
async fn get_user_wallet_success_and_rejection() {
    let query = GetUserWalletQuery::default();
    let (url, received) = support::serve(200, r#"[]"#).await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.get_user_wallet(&query).await;
    assert!(
        result.is_ok(),
        "success fixture for GET /api/v1/user/wallet: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(request.starts_with("GET /api/v1/user/wallet"), "{request}");
    assert!(request.contains("api-signature:"), "{request}");

    let (url, received) = support::serve(
        400,
        r#"{"error":{"name":"ValidationError","message":"fixture"}}"#,
    )
    .await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.get_user_wallet(&query).await;
    assert!(
        matches!(
            result,
            Err(bitmex_client::OperationError::Rejected { status: 400, .. })
        ),
        "rejection fixture: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(request.starts_with("GET /api/v1/user/wallet"), "{request}");
}

#[tokio::test]
async fn get_wallet_history_success_and_rejection() {
    let query = GetWalletHistoryQuery::default();
    let (url, received) = support::serve(200, r#"[]"#).await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.get_wallet_history(&query).await;
    assert!(
        result.is_ok(),
        "success fixture for GET /api/v1/user/walletHistory: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("GET /api/v1/user/walletHistory"),
        "{request}"
    );
    assert!(request.contains("api-signature:"), "{request}");

    let (url, received) = support::serve(
        400,
        r#"{"error":{"name":"ValidationError","message":"fixture"}}"#,
    )
    .await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.get_wallet_history(&query).await;
    assert!(
        matches!(
            result,
            Err(bitmex_client::OperationError::Rejected { status: 400, .. })
        ),
        "rejection fixture: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("GET /api/v1/user/walletHistory"),
        "{request}"
    );
}

#[tokio::test]
async fn get_wallet_summary_success_and_rejection() {
    let query = GetWalletSummaryQuery::default();
    let (url, received) = support::serve(200, r#"[]"#).await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.get_wallet_summary(&query).await;
    assert!(
        result.is_ok(),
        "success fixture for GET /api/v1/user/walletSummary: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("GET /api/v1/user/walletSummary"),
        "{request}"
    );
    assert!(request.contains("api-signature:"), "{request}");

    let (url, received) = support::serve(
        400,
        r#"{"error":{"name":"ValidationError","message":"fixture"}}"#,
    )
    .await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.get_wallet_summary(&query).await;
    assert!(
        matches!(
            result,
            Err(bitmex_client::OperationError::Rejected { status: 400, .. })
        ),
        "rejection fixture: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("GET /api/v1/user/walletSummary"),
        "{request}"
    );
}

#[tokio::test]
async fn wallet_transfer_success_and_rejection() {
    let body: WalletTransferBody = serde_json::from_str(
        r#"{"amount":1,"currency":"fixture","fromAccountId":1,"targetAccountId":1}"#,
    )
    .expect("body fixture");
    let (url, received) = support::serve(200, r#"{}"#).await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.wallet_transfer(&body).await;
    assert!(
        result.is_ok(),
        "success fixture for POST /api/v1/user/walletTransfer: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("POST /api/v1/user/walletTransfer"),
        "{request}"
    );
    assert!(request.contains("api-signature:"), "{request}");

    let (url, received) = support::serve(
        400,
        r#"{"error":{"name":"ValidationError","message":"fixture"}}"#,
    )
    .await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.wallet_transfer(&body).await;
    assert!(
        matches!(
            result,
            Err(bitmex_client::OperationError::Rejected { status: 400, .. })
        ),
        "rejection fixture: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("POST /api/v1/user/walletTransfer"),
        "{request}"
    );
}

#[tokio::test]
async fn user_affiliates_get_success_and_rejection() {
    let query = UserAffiliatesGetQuery::default();
    let (url, received) = support::serve(200, r#"[]"#).await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.user_affiliates_get(&query).await;
    assert!(
        result.is_ok(),
        "success fixture for GET /api/v1/userAffiliates: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("GET /api/v1/userAffiliates"),
        "{request}"
    );
    assert!(request.contains("api-signature:"), "{request}");

    let (url, received) = support::serve(
        400,
        r#"{"error":{"name":"ValidationError","message":"fixture"}}"#,
    )
    .await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.user_affiliates_get(&query).await;
    assert!(
        matches!(
            result,
            Err(bitmex_client::OperationError::Rejected { status: 400, .. })
        ),
        "rejection fixture: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("GET /api/v1/userAffiliates"),
        "{request}"
    );
}

#[tokio::test]
async fn user_event_get_success_and_rejection() {
    let query = UserEventGetQuery::default();
    let (url, received) = support::serve(200, r#"[]"#).await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.user_event_get(&query).await;
    assert!(
        result.is_ok(),
        "success fixture for GET /api/v1/userEvent: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(request.starts_with("GET /api/v1/userEvent"), "{request}");
    assert!(request.contains("api-signature:"), "{request}");

    let (url, received) = support::serve(
        400,
        r#"{"error":{"name":"ValidationError","message":"fixture"}}"#,
    )
    .await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.user_event_get(&query).await;
    assert!(
        matches!(
            result,
            Err(bitmex_client::OperationError::Rejected { status: 400, .. })
        ),
        "rejection fixture: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(request.starts_with("GET /api/v1/userEvent"), "{request}");
}

#[tokio::test]
async fn user_price_alert_get_alerts_success_and_rejection() {
    let query = UserPriceAlertGetAlertsQuery::default();
    let (url, received) = support::serve(200, r#"[]"#).await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.user_price_alert_get_alerts(&query).await;
    assert!(
        result.is_ok(),
        "success fixture for GET /api/v1/userPriceAlert: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("GET /api/v1/userPriceAlert"),
        "{request}"
    );
    assert!(request.contains("api-signature:"), "{request}");

    let (url, received) = support::serve(
        400,
        r#"{"error":{"name":"ValidationError","message":"fixture"}}"#,
    )
    .await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.user_price_alert_get_alerts(&query).await;
    assert!(
        matches!(
            result,
            Err(bitmex_client::OperationError::Rejected { status: 400, .. })
        ),
        "rejection fixture: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("GET /api/v1/userPriceAlert"),
        "{request}"
    );
}

#[tokio::test]
async fn user_price_alert_create_alert_success_and_rejection() {
    let (url, received) = support::serve(
        200,
        r#"{"userId":1,"symbol":"fixture","alertType":"fixture"}"#,
    )
    .await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.user_price_alert_create_alert().await;
    assert!(
        result.is_ok(),
        "success fixture for POST /api/v1/userPriceAlert: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("POST /api/v1/userPriceAlert"),
        "{request}"
    );
    assert!(request.contains("api-signature:"), "{request}");

    let (url, received) = support::serve(
        400,
        r#"{"error":{"name":"ValidationError","message":"fixture"}}"#,
    )
    .await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.user_price_alert_create_alert().await;
    assert!(
        matches!(
            result,
            Err(bitmex_client::OperationError::Rejected { status: 400, .. })
        ),
        "rejection fixture: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("POST /api/v1/userPriceAlert"),
        "{request}"
    );
}

#[tokio::test]
async fn user_price_alert_update_alert_success_and_rejection() {
    let id = PathId::new("fixture").expect("path id");
    let (url, received) = support::serve(
        200,
        r#"{"userId":1,"symbol":"fixture","alertType":"fixture"}"#,
    )
    .await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.user_price_alert_update_alert(&id).await;
    assert!(
        result.is_ok(),
        "success fixture for PUT /api/v1/userPriceAlert/fixture: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("PUT /api/v1/userPriceAlert/fixture"),
        "{request}"
    );
    assert!(request.contains("api-signature:"), "{request}");

    let (url, received) = support::serve(
        400,
        r#"{"error":{"name":"ValidationError","message":"fixture"}}"#,
    )
    .await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.user_price_alert_update_alert(&id).await;
    assert!(
        matches!(
            result,
            Err(bitmex_client::OperationError::Rejected { status: 400, .. })
        ),
        "rejection fixture: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("PUT /api/v1/userPriceAlert/fixture"),
        "{request}"
    );
}

#[tokio::test]
async fn get_volume_rank_success_and_rejection() {
    let query = GetVolumeRankQuery::default();
    let (url, received) = support::serve(200, r#"{}"#).await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.get_volume_rank(&query).await;
    assert!(
        result.is_ok(),
        "success fixture for GET /api/v1/userStats/volumeRank: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("GET /api/v1/userStats/volumeRank"),
        "{request}"
    );
    assert!(request.contains("api-signature:"), "{request}");

    let (url, received) = support::serve(
        400,
        r#"{"error":{"name":"ValidationError","message":"fixture"}}"#,
    )
    .await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.get_volume_rank(&query).await;
    assert!(
        matches!(
            result,
            Err(bitmex_client::OperationError::Rejected { status: 400, .. })
        ),
        "rejection fixture: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("GET /api/v1/userStats/volumeRank"),
        "{request}"
    );
}

#[tokio::test]
async fn get_wallet_assets_success_and_rejection() {
    let (url, received) = support::serve(200, r#"[]"#).await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.get_wallet_assets().await;
    assert!(
        result.is_ok(),
        "success fixture for GET /api/v1/wallet/assets: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("GET /api/v1/wallet/assets"),
        "{request}"
    );
    assert!(request.contains("api-signature:"), "{request}");

    let (url, received) = support::serve(
        400,
        r#"{"error":{"name":"ValidationError","message":"fixture"}}"#,
    )
    .await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.get_wallet_assets().await;
    assert!(
        matches!(
            result,
            Err(bitmex_client::OperationError::Rejected { status: 400, .. })
        ),
        "rejection fixture: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("GET /api/v1/wallet/assets"),
        "{request}"
    );
}

#[tokio::test]
async fn get_wallet_currencies_success_and_rejection() {
    let (url, received) = support::serve(200, r#"[]"#).await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.get_wallet_currencies().await;
    assert!(
        result.is_ok(),
        "success fixture for GET /api/v1/wallet/currencies: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("GET /api/v1/wallet/currencies"),
        "{request}"
    );
    assert!(request.contains("api-signature:"), "{request}");

    let (url, received) = support::serve(
        400,
        r#"{"error":{"name":"ValidationError","message":"fixture"}}"#,
    )
    .await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.get_wallet_currencies().await;
    assert!(
        matches!(
            result,
            Err(bitmex_client::OperationError::Rejected { status: 400, .. })
        ),
        "rejection fixture: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("GET /api/v1/wallet/currencies"),
        "{request}"
    );
}

#[tokio::test]
async fn get_wallet_conversion_haircut_success_and_rejection() {
    let (url, received) = support::serve(200, r#"[]"#).await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.get_wallet_conversion_haircut().await;
    assert!(
        result.is_ok(),
        "success fixture for GET /api/v1/wallet/haircuts: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("GET /api/v1/wallet/haircuts"),
        "{request}"
    );
    assert!(request.contains("api-signature:"), "{request}");

    let (url, received) = support::serve(
        400,
        r#"{"error":{"name":"ValidationError","message":"fixture"}}"#,
    )
    .await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.get_wallet_conversion_haircut().await;
    assert!(
        matches!(
            result,
            Err(bitmex_client::OperationError::Rejected { status: 400, .. })
        ),
        "rejection fixture: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("GET /api/v1/wallet/haircuts"),
        "{request}"
    );
}

#[tokio::test]
async fn get_wallet_networks_success_and_rejection() {
    let (url, received) = support::serve(200, r#"[]"#).await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.get_wallet_networks().await;
    assert!(
        result.is_ok(),
        "success fixture for GET /api/v1/wallet/networks: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("GET /api/v1/wallet/networks"),
        "{request}"
    );
    assert!(request.contains("api-signature:"), "{request}");

    let (url, received) = support::serve(
        400,
        r#"{"error":{"name":"ValidationError","message":"fixture"}}"#,
    )
    .await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.get_wallet_networks().await;
    assert!(
        matches!(
            result,
            Err(bitmex_client::OperationError::Rejected { status: 400, .. })
        ),
        "rejection fixture: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("GET /api/v1/wallet/networks"),
        "{request}"
    );
}

#[tokio::test]
async fn cancel_order_v2_success_and_rejection() {
    let body: CancelOrderV2Body =
        serde_json::from_str(r#"{"orderID":["fixture"]}"#).expect("body fixture");
    let (url, received) = support::serve(200, r#"[]"#).await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.cancel_order_v2(&body).await;
    assert!(
        result.is_ok(),
        "success fixture for DELETE /api/v2/order: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(request.starts_with("DELETE /api/v2/order"), "{request}");
    assert!(request.contains("api-signature:"), "{request}");

    let (url, received) = support::serve(
        400,
        r#"{"error":{"name":"ValidationError","message":"fixture"}}"#,
    )
    .await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.cancel_order_v2(&body).await;
    assert!(
        matches!(
            result,
            Err(bitmex_client::OperationError::Rejected { status: 400, .. })
        ),
        "rejection fixture: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(request.starts_with("DELETE /api/v2/order"), "{request}");
}

#[tokio::test]
async fn new_order_v2_success_and_rejection() {
    let body: NewOrderV2Body = serde_json::from_str(
        r#"{"symbol":"fixture","side":"Buy","ordType":"Limit","orderQty":1,"price":1}"#,
    )
    .expect("body fixture");
    let (url, received) = support::serve(200, r#"{"account":1,"cumQty":1,"leavesQty":1,"orderID":"fixture","orderQty":1,"side":"Buy","symbol":"fixture"}"#).await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.new_order_v2(&body).await;
    assert!(
        result.is_ok(),
        "success fixture for POST /api/v2/order: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(request.starts_with("POST /api/v2/order"), "{request}");
    assert!(request.contains("api-signature:"), "{request}");

    let (url, received) = support::serve(
        400,
        r#"{"error":{"name":"ValidationError","message":"fixture"}}"#,
    )
    .await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.new_order_v2(&body).await;
    assert!(
        matches!(
            result,
            Err(bitmex_client::OperationError::Rejected { status: 400, .. })
        ),
        "rejection fixture: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(request.starts_with("POST /api/v2/order"), "{request}");
}

#[tokio::test]
async fn amend_order_v2_success_and_rejection() {
    let body: AmendOrderV2Body =
        serde_json::from_str(r#"{"orderID":"fixture"}"#).expect("body fixture");
    let (url, received) = support::serve(200, r#"{"account":1,"cumQty":1,"leavesQty":1,"orderID":"fixture","orderQty":1,"side":"Buy","symbol":"fixture"}"#).await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.amend_order_v2(&body).await;
    assert!(
        result.is_ok(),
        "success fixture for PUT /api/v2/order: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(request.starts_with("PUT /api/v2/order"), "{request}");
    assert!(request.contains("api-signature:"), "{request}");

    let (url, received) = support::serve(
        400,
        r#"{"error":{"name":"ValidationError","message":"fixture"}}"#,
    )
    .await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.amend_order_v2(&body).await;
    assert!(
        matches!(
            result,
            Err(bitmex_client::OperationError::Rejected { status: 400, .. })
        ),
        "rejection fixture: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(request.starts_with("PUT /api/v2/order"), "{request}");
}

#[tokio::test]
async fn cancel_all_order_v2_success_and_rejection() {
    let body: CancelAllOrderV2Body = serde_json::from_str(r#"{}"#).expect("body fixture");
    let (url, received) = support::serve(200, r#"[]"#).await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.cancel_all_order_v2(&body).await;
    assert!(
        result.is_ok(),
        "success fixture for DELETE /api/v2/order/all: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(request.starts_with("DELETE /api/v2/order/all"), "{request}");
    assert!(request.contains("api-signature:"), "{request}");

    let (url, received) = support::serve(
        400,
        r#"{"error":{"name":"ValidationError","message":"fixture"}}"#,
    )
    .await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.cancel_all_order_v2(&body).await;
    assert!(
        matches!(
            result,
            Err(bitmex_client::OperationError::Rejected { status: 400, .. })
        ),
        "rejection fixture: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(request.starts_with("DELETE /api/v2/order/all"), "{request}");
}

#[tokio::test]
async fn cancel_all_after_order_v2_success_and_rejection() {
    let body: CancelAllAfterOrderV2Body = serde_json::from_str(r#"{}"#).expect("body fixture");
    let (url, received) = support::serve(200, r#"[]"#).await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.cancel_all_after_order_v2(&body).await;
    assert!(
        result.is_ok(),
        "success fixture for POST /api/v2/order/cancelAllAfter: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("POST /api/v2/order/cancelAllAfter"),
        "{request}"
    );
    assert!(request.contains("api-signature:"), "{request}");

    let (url, received) = support::serve(
        400,
        r#"{"error":{"name":"ValidationError","message":"fixture"}}"#,
    )
    .await;
    let client = Client::builder(Environment::Testnet)
        .credentials(ApiCredentials::new("fixture-key", "fixture-secret").expect("credentials"))
        .loopback_rest_url(url)
        .build()
        .expect("client");
    let result = client.cancel_all_after_order_v2(&body).await;
    assert!(
        matches!(
            result,
            Err(bitmex_client::OperationError::Rejected { status: 400, .. })
        ),
        "rejection fixture: {result:?}"
    );
    let request = received.await.expect("request recorded");
    assert!(
        request.starts_with("POST /api/v2/order/cancelAllAfter"),
        "{request}"
    );
}
